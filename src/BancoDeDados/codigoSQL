create database db_projeto;
use db_projeto;

CREATE TABLE Tarefa (
idT INT AUTO_INCREMENT PRIMARY KEY,
titulo VARCHAR(50),
descricao VARCHAR(250),
dataPrazo VARCHAR(20),
notificacoes boolean
);

select * from Tarefa;

SELECT * FROM Tarefa WHERE id=2;
